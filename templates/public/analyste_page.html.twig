{% extends 'base.html.twig' %}

{% block title %}
    {{ pro.user.fullName }}
{% endblock %}

{% block top %}
    <section class="hero-wrap js-fullheight" style="background-image: url({{ asset('images/bg_3.jpg') }});">
        <div class="overlay"></div>
        <div class="container-fluid px-0">
            <div class="row no-gutters text align-items-end js-fullheight justify-content-center"
                 data-scrollax-parent="true">
                <div class="col-md-12 ftco-animate text-center">
                    <p class="breadcrumbs"><span class="mr-2"><a href="{{ path('app_consult_pro_page', {'id':pro.id}) }}">Découvrir l'accompagnement</a></span>
                        <span>{{ pro.user.fullName }}</span></p>
                    <h1 class="bread">{{ pro.user.fullName }}</h1>
                </div>
            </div>
        </div>
    </section>
{% endblock %}

{% block body %}
    <div class="m-5">
        {{ pro.pageContent | raw }}
    </div>
    {#
    <div class="d-flex justify-content-center text-center m-5">
        <a class="btn btn-primary" href="{{ path('app_scheduler', {'id':pro.id}) }}">Prendre rendez vous</a>
    </div>
    #}
{% endblock %}